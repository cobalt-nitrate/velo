{
  "_comment": "Phase 3 — required fields per tool for extraction completeness (overrides code defaults when set).",
  "tool_required_fields": {
    "data.leave_records.create": ["employee_id", "from_date", "to_date"],
    "data.leave_records.update_status": ["record_id", "status"],
    "data.employees.create": ["full_name", "email", "employee_id"],
    "data.employees.update": ["employee_id"],
    "data.compliance_calendar.mark_done": ["calendar_id"],
    "data.tax_obligations.create": ["obligation_id", "type", "due_date"],
    "data.bank_transactions.create": ["date", "narration", "amount"],
    "data.policy_documents.create": ["doc_id", "doc_type", "content_markdown"],
    "data.hr_tasks.create": ["task_id", "employee_id", "task_type"],
    "data.hr_tasks.update_status": ["task_id", "status"]
  }
}
