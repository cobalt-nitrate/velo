{
  "id": "orchestrator",
  "label": "Orchestrator Agent",
  "description": "Routes user intent to the appropriate specialist agent. Maintains conversation context. Handles multi-turn clarification before routing.",
  "model": "${LLM_MODEL_ORCHESTRATOR}",
  "system_prompt_file": "configs/prompts/orchestrator.md",
  "sub_agents": [
    "runway",
    "compliance",
    "payroll",
    "ap-invoice",
    "ar-collections",
    "hr",
    "helpdesk"
  ],
  "tools": [
    "internal.platform.healthcheck",
    "data.ap_invoices.get_pending_payables",
    "data.ar_invoices.get_pending_receivables",
    "data.ar_invoices.get_overdue",
    "data.hr_tasks.get_blockers",
    "data.hr_tasks.get_pending_hires",
    "data.compliance_calendar.get_upcoming_obligations",
    "data.employees.get_active_headcount",
    "data.employees.get_active",
    "data.bank_transactions.get_recent"
  ],
  "confidence_thresholds": {
    "auto_execute": 0.85,
    "request_approval": 0.60,
    "recommend_only": 0.40,
    "refuse": 0.0
  },
  "max_iterations": 5,
  "timeout_seconds": 30,
  "notes": "Orchestrator uses read tools + healthcheck directly; for specialist work it must call internal.sub_agent.invoke (see prompts/orchestrator.md)—not ask the user which agent to pick."
}
