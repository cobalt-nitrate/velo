{
  "id": "runway_liquidity_snapshot",
  "label": "Runway — latest bank balance",
  "description": "Phase 2 minimum path: read consolidated cash signal from bank_transactions.",
  "trigger": "runway.liquidity.refresh",
  "required_params": [],
  "steps": [
    {
      "step": 1,
      "id": "read_balance",
      "action": "tool_call",
      "tool": "data.bank_transactions.get_latest_balance",
      "input_from": "trigger.payload",
      "output_to": "liquidity"
    }
  ]
}
